services:
  web:
    image: ubuntu:22.04

    commands:
      init:
        - apt-get update
        - apt-get install -y apache2
        - echo "<h1>FUNCIONOU</h1>" > /var/www/html/index.html

      start:
        - /usr/sbin/apache2ctl -D FOREGROUND

    expose: 80
