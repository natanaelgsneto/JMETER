services:
  web:
    image: ubuntu:22.04
    default: true

    commands:
      build:
        - apt update
        - apt install -y git nginx

      start:
        - nginx -g "daemon off;"

    ports:
      - 80
